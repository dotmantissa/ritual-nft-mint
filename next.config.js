/** @type {import('next').NextConfig} */
const nextConfig = {
  // Empty turbopack config silences the Turbopack/webpack conflict warning (Next.js 16+)
  turbopack: {},
};

module.exports = nextConfig;
